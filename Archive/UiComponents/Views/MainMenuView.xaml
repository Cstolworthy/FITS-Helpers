<UserControl x:Class="UiComponents.Views.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Menu Height="22" Name="menu1" Margin="0,0,0,0" HorizontalAlignment="Stretch" VerticalAlignment="Top" BorderThickness="2">
            <Menu.BitmapEffect>
                <DropShadowBitmapEffect />
            </Menu.BitmapEffect>
            <MenuItem Header="_File">
                <MenuItem Header="_Exit" Command="{Binding ExitApplicationCommand}"></MenuItem>
            </MenuItem>
            <MenuItem Header="_Utilities">
                <MenuItem Header="_Import FITS Image" Command="{Binding ImportFitsImageCommand}"></MenuItem>
            </MenuItem>
        </Menu>
    </Grid>
</UserControl>
